<?php
// Exit if accessed directly.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Main plugin class for TH Login.
 * Handles core plugin functionality, including activation, deactivation, and initializing other components.
 */
class TH_Login_Core { // Renamed to TH_Login_Core to avoid conflict with main TH_Login class

	/**
	 * Constructor.
	 */
	public function __construct() {
		// This class primarily acts as a placeholder for core logic if needed,
		// but the main plugin file's TH_Login class already handles most of this.
		// For now, it's included for structure.
	}
}
